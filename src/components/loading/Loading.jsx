import React from "react";

const Loading = () => {
    return (
        <div className="w-full h-screen text-2xl bg-white text-black flex items-center justify-center">
            Loading...
        </div>
    );
};

export default Loading;
